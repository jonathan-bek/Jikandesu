@{ Layout = "~/Shared/_Layout.cshtml"; }
@{ ViewBag.Title = "Manga"; }

<body ng-app="mangaApp">
    <div ng-controller="mangaCtrl as ctrl">
        <div class="col-xs-12">
            <div class="col-xs-0 col-sm-3"></div>
            <div class="col-xs-12 col-sm-6 text-center">
                <h2>Manga Page</h2>
                <div ng-repeat="p in ctrl.mangaPages">
                    <p>{{p.Title}}</p>
                    <p>{{p.Url}}</p>
                </div>
                @*<img ng-src="{{ctrl.mangaPage.ImageUrl}}" />
                    <h4>{{ctrl.mangaPage.Title}}</h4>
                    <button ng-show="ctrl.mangaPage !== undefined"
                            ng-click="ctrl.saveMangaPage()"
                            class="btn btn-success">
                        Save
                    </button>
                    <table class="text-left">
                        <tr>
                            <th>Chapters</th>
                            <th>Views</th>
                            <th>Uploaded</th>
                        </tr>
                        <tr ng-repeat="c in ctrl.mangaPage.MangaChapters">
                            <td>
                                <a ng-href="{{c.ChapterUrl}}">{{c.ChapterName}}</a>
                            </td>
                            <td class="text-right">
                                {{c.ChapterViews}}
                            </td>
                            <td>
                                {{c.ChapterUploadDateString}}
                            </td>
                        </tr>
                    </table>*@
            </div>
        </div>
    </div>
</body>

<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="~/Scripts/angular.min.js"></script>
<script src="~/Scripts/Manga/MangaBundle.js"></script>
